def add_two_numbers(num1: float, num2:float=10) -> float:

    """
    def add_em(num_1: float, num_2: float):
   	'''
   	Adds two numbers together.
   	Args:
       		num_1(float): the first number in the addition operation.
      		num_2(float): the second number in the addition operation.
   	Returns:
      	 float: the sum of the two numbers
   	'''
   	return num_1 + num_2

    """
    return num1 + num2

result1 = add_two_numbers(4, 9)
result2 = add_two_numbers (1, -10)
result3 = add_two_numbers (9.25, 6.6)

print(result1, result2, result3)
